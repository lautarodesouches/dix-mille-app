const dicesUrl = '../../assets/images/'

const DICES_IMAGES = [
    require(dicesUrl + '1.png'),
    require(dicesUrl + '2.png'),
    require(dicesUrl + '3.png'),
    require(dicesUrl + '4.png'),
    require(dicesUrl + '5.png'),
    require(dicesUrl + '6.png')
]

export default DICES_IMAGES